# Frontend Environment Variables

# Backend API URL (for browser-side requests)
# This is the URL that the user's browser will use to make requests
# When running with docker-compose: use http://localhost:8000 or http://127.0.0.1:8000
# When running frontend separately: use http://localhost:8000 or http://127.0.0.1:8000
REACT_APP_API_URL=http://localhost:8000

# Backend proxy target (for server-side proxy requests from Node.js dev server)
# This is the URL that the Node.js proxy will use to forward requests to the backend
# When running in Docker with docker-compose: use http://api:8000 (Docker service name)
# When running locally outside Docker: use http://localhost:8000 (or omit, it's the default)
# DEFAULT: http://localhost:8000
# REACT_APP_PROXY_TARGET=http://api:8000

# Enable polling for file watching in Docker
CHOKIDAR_USEPOLLING=true

# WebSocket port for hot reloading
WDS_SOCKET_PORT=3000
